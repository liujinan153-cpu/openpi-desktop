# OpenPi Desktop 工程记忆（pi 自动加载）

## 当前进度快照（2026-09-16，会话上下文接力锚点）
- 版本 **0.56.0**（**P67 UI 第二波：minimap 对话轨道 + 设置全局搜索**，e2e-p67 8断言，e2e-all **38 套**）（0.55.0：P66 渐进流式+思考展开——修「流式不实时显示」报障，实测链路通畅根因长思考期折叠条正文空白，思考流式自动展开；e2e-p66 6断言）（0.54.0：P65 通知中心+Ctrl+K 命令面板）（0.53.x：P64 并行 worker 多角色+会话导入、结论回喂竞态修复；P63 lsp_diag+Goal；P62 repo map/ast_edit/run_tests）（坐坑 #119：e2e mock 必须分块慢发才测得出流式渲染；#118：mock 缺 [DONE]+end 则 SDK 挂流；#117：worker 结论回喂=延迟注入+三态）
- **贴图 400 排查结论**（详见 PROGRESS.md P38.5 段）：1210=[text(""),image] 组合被智谱拒收（婚纱照 7.3MB 直发正常，非尺寸问题）；1214=空 data 图片（pi Desktop 上游自身附件链路问题，OpenPi 链路干净）；Connection error=到 open.bigmodel.cn 网络间歇不通，与 400 无关
- **发布流程**：`npm run dist`（全量 e2e-all 24 套门槛，大版本用）| `node scripts/dist.mjs --fast`（小改动快发，跳过回归；受影响的套件用 `e2e-all --only <名>` 单跑）→ `node scripts/publish.mjs`；发布源 E:/pi2/openpi-releases @9355（VBS 自启）；e2e 与发布源端口隔离；**用户指示：改动小就不跑全量回归（耗时且费 token），全量留给大版本**
- **待办**：① 0.34.0 发布后用户真机验证「只贴图不写字」② 公网发布（github provider 或公网 HTTP 二选一）③ 观察文件卡误伤率 ④ 存量 0.32.0 用户升级需手动跑一次新 Setup（#86 跨坑）
- 踩坑已至 #115（#89 智谱拒收组合；#90 taskkill //PID；#91 常驻实例杀净+netstat 复查；#92 SKILL.md 安装引导；#93 PATH 尾部追加不前插——前插抢内置 Python 运行时优先级；#94 worker 内 userDataDir 必须 boot 消息注入——P41 沙箱依赖；#95 electron-rebuild 缺 Python → PYTHON=resources/runtime/python/python3.exe，rebuild 后为 electron ABI 勿被 node require；#96 JSON 清洗正则 ,s*[]}] 才对——清洗类正则必须打印结果验证；#97 index.html 标签错配 <div></span> → div 链错位、tab 嵌套隐藏——静态扫标签配对 + 运行时量卡片渲染高度双保险，querySelectorAll 断言不查可见性抓不到；#98 私有方法 #ensure() 无返回值——walk(this.#ensure().sessionManager) 拿到 undefined 直接抛错，必须分开写 this.#ensure(); this.session.xxx（e2e-p47 抓住）；#99 子代理 createAgentSession 不传 agentDir → SDK 回退真目录配置致 baseUrl/auth 脱节 401 静默失败（报错藏 message_end.errorMessage），且默认链错误会被主会话 fallback 掩盖；#100 批量 patch 锚点引号不匹配时 replace 无匹配仍报 patched——sed/replace 后必须 grep 验证（#96 再现）；#101 CRI({target}) 必须带 port 否则静默回落 9222；#102 e2e 经 CDP 调 async 函数时 Runtime.evaluate 会等 promise settle——fire-and-forget 用同步 IIFE 包裹，且渲染层审批假拒实为 e2e 卡死所致；#103 渲染层 innerHTML 把快照里的标签字面量吃掉——断言只锚转义安全文本；#104 chrome-remote-interface 放 devDependencies 打包不进 asar——浏览器工具运行时报 Cannot find package，源码/e2e 测不出（node_modules 齐全），**打包后必须验证 asar 内依赖**；#105 SDK ctx.cwd=进程 cwd 不是 workspace——工具 cwd 必须 setGitWorkspace 显式注入，且 boot() 硬编码 startSession(null) 任务模式，e2e 需 ev 驱动 startSession(workspace)；#106 custom tool execute 真实签名是 (toolCallId, params, …)——第一个不需要参数的工具看不出错，第一个吃 params 的工具直接拿错位（memory_write 抓住），必须 execute(_id, params = {}))；#107 PowerShell 5.1 无 BOM 的 UTF8 中文注释会打乱解析——ps1 必须 UTF8 BOM；#108 SendKeys 的 shift 组合选择/方向键在 UWP 记事本失效——方向键是扩展键，keybd_event 必须带 EXTENDEDKEY flag，选区用真实 shift+tap 组合；#109 Win11 商店版记事本单实例+标签页：spawn notepad.exe 只会往已有窗口加 tab，e2e 靶子窗口用独立 electron 实例替代；#110 node --check 不支持 ESM（.mjs/type:module 全线误报）——统一走 verify-check.cjs（按扩展名+package.json type 判模块类型，ESM 用 --input-type=module --check 走 stdin）；#111 Chromium 系应用 UIA 树需 a11y 触发后延迟就绪（elements 全匿名时等 2.5s 重试）；审批弹窗会抢前台——写类电脑工具审批通过后前台在本应用，type/key 前必须先 activate 目标窗口；#112 混合 DPI 多屏下非 DPI-aware 进程 SetCursorPos 与 UIA 坐标系偏差数十像素——daemon 必须 SetProcessDpiAwarenessContext(PER_MONITOR_V2)；bash 内嵌 node -e 的正则替换第三次失败（#100 家族）——一律用 edit 工具；#114 fast 发布通道跳过旧套件回归=盲区（p49 子代理审批误拦坏了几版才被全量抓到）——小版本也该隔几版跑一次全量；#115 ast-grep 路径参数是 glob 语义（Windows 反斜杠=转义符→静默 0 匹配），传参必须正斜杠；Applied N changes 在 stderr，execFileSync 拿不到要用 spawnSync；#113 动手前先 grep 项目内已有实现（P61 想做 MCP 桥，P28 早已做完，重复造轮子半天才盘点出来）；**#117 worker 会话阻塞等待会卡死主会话**——tool execute 内 await 子 LLM 流（哪怕包一层 Promise.all）触发 SDK 轮转竞态，主会话 tool result 后不再回喂，对照实验实锤：同工具同参数，直接 await 通 / Promise.all([p]) 挂；**唯一稳定形态：后台跑 + 完成后 session.prompt(结论,{streamingBehavior:"followUp"})**（followUp 队列只在 loop 收尾时消费不自唤醒；并发 prompt 撞重入保护时错误消息明示 streamingBehavior）；**内置 bash 不能给 worker 会话**（激活即卡死主会话），用自定义 run_cmd 替代；session 隔离用 PI_HOME/OPENPI_SESSIONS_ROOT env——**USERPROFILE 覆写会让 Electron 静默死**
